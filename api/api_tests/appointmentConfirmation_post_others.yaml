# ********RoostGPT********

# Test generated by RoostGPT for test artillery-unittest using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /appointmentConfirmation_post for http method type POST in artillery framework
# RoostTestHash=1705b22338
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "appointmentConfirmation_post_others.csv"
    fields:
      - "confirmation"
      - "id"
      - "subscriberId"
    skipHeader: true
    delimiter: "^|^"
  defaults:
    headers:
      Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Test non-success API responses"
    flow:
      - log: "Sending an invalid POST request to /appointmentConfirmation"
      - post:
          url: "/appointmentConfirmation_invalid"
          json:
            appointment:
              confirmation: "{{ confirmation }}"
              id: "{{ id }}"
            customer:
              subscriberId: "{{ subscriberId }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1
      - log: "Sending a POST request to /appointmentConfirmation without authorization"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Authorization: ""
          json:
            appointment:
              confirmation: "{{ confirmation }}"
              id: "{{ id }}"
            customer:
              subscriberId: "{{ subscriberId }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1
      - log: "Sending a POST request to /appointmentConfirmation with invalid payload"
      - post:
          url: "/appointmentConfirmation"
          json:
            appointment:
              confirmation: "invalid"
              id: "{{ id }}"
            customer:
              subscriberId: "{{ subscriberId }}"
          expect:
            - statusCode: 422
            - hasProperty: "error"
      - think: 1
