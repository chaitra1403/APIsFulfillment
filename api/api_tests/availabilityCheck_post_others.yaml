# ********RoostGPT********

# Test generated by RoostGPT for test api-artillery using AI Type  and AI Model 
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=9268d8aa92
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "availabilityCheck_post_others.csv"
    fields: ["field1", "field2"]
    skipHeader: true
    delimiter: "^|^"
  defaults:
    headers:
      Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Test Scenario for Non-Success API Responses"
    flow:
      - log: "Testing POST /availabilityCheck with valid payload"
      - post:
          url: "/availabilityCheck"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          expect:
            - statusCode: 201
            - hasProperty: "result"
      - think: 1

      - log: "Testing POST /availabilityCheck with invalid payload"
      - post:
          url: "/availabilityCheck"
          json:
            field1: "invalid"
            field2: "invalid"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Testing POST /availabilityCheck with missing Authorization header"
      - post:
          url: "/availabilityCheck"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          headers:
            Authorization: ""
          expect:
            - statusCode: 401
      - think: 1

      - log: "Testing POST /availabilityCheck with invalid URL"
      - post:
          url: "/invalidURL"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          expect:
            - statusCode: 404
      - think: 1
