# ********RoostGPT********

# Test generated by RoostGPT for test api-improve-test using AI Type  and AI Model 
# 
# Test generated for /query_get for http method type GET in artillery framework
# RoostTestHash=ccd1119a9e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "query_get_others.csv"
    fields:
      - "symbol"
      - "apikey"
      - "function"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "https://www.alphavantage.co"

scenarios:
  - name: "Test Non-Success API Responses"
    flow:
      - log: "Starting scenario for non-success API responses"
      - post:
          url: "/query"
          headers:
            Content-Type: "application/json"
            name: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            symbol: "{{ symbol }}"
            apikey: "{{ apikey }}"
            function: "{{ function }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1
      - get:
          url: "/invalid-url"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Ending scenario for non-success API responses"
