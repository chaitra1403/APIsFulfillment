# ********RoostGPT********

# Test generated by RoostGPT for test Artillery-test2-unit-swagger using AI Type  and AI Model 
# 
# Test generated for /laureate/{laureateID}_get for http method type GET in artillery framework
# RoostTestHash=56bfcadf64
# 
# 

# ********RoostGPT********
config:
  target: "{{ dev.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "laureate_laureateID_get_others.csv"
    fields:
      - "laureateID"
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      API_HOST: "your_api_host_here"
      AUTH_TOKEN: "your_auth_token_here"
      target: "{{ dev.API_HOST }}"
      http:
        timeout: 10

scenarios:
  - name: Test Non-Success API Responses
    flow:
      - log: "Starting invalid laureateID test"
      - get:
          url: "/2.1/laureate/{{ laureateID }}"
          headers:
            Authorization: "Bearer {{ dev.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: 'error'
          capture:
            json: "$.error"
            as: "errorMessage"
      - think: 1

      - log: "Starting test with missing Authorization token"
      - get:
          url: "/2.1/laureate/{{ laureateID }}"
          headers:
            Authorization: ""
          expect:
            - statusCode: 401
            - hasProperty: 'error'
          capture:
            json: "$.error"
            as: "errorMessage"
      - think: 1

      - log: "Starting test with incorrect API endpoint"
      - get:
          url: "/2.1/invalid/{{ laureateID }}"
          headers:
            Authorization: "Bearer {{ dev.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: 'error'
          capture:
            json: "$.error"
            as: "errorMessage"
      - think: 1
