# ********RoostGPT********

# Test generated by RoostGPT for test Artillery-test1-unit using AI Type  and AI Model 
# 
# Test generated for /products/{productId}_delete for http method type DELETE in artillery framework
# RoostTestHash=befb82c928
# 
# 

# ********RoostGPT********
config:
  target: "{{$processEnvironment.API_HOST}}"
  phases:
    - duration: 1
      arrivalRate: 1
  payload:
    path: "products_productId_delete_success.csv"
    fields:
      - "productId"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: Delete a product
    flow:
      - log: "Starting the delete product scenario"
      - delete:
          url: "/products/{{productId}}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "message"
            - equals:
                - "message"
                - "Product deleted"
      - log: "Product with ID {{productId}} deleted successfully"
      - think: 2
