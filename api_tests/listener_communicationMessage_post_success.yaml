# ********RoostGPT********

# Test generated by RoostGPT for test new-postman_clone using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /listener/communicationMessage_post for http method type POST in artillery framework
# RoostTestHash=fdd5ae6c84
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "listener_communicationMessage_post_success.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Communication Message Listener Scenario"
    flow:
      - log: "Starting POST request to /listener/communicationMessage for incident opening notification"
      - post:
          url: "/listener/communicationMessage"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            affectedService:
              address:
                - city: "{{ city }}"
                  cityCode: "{{ cityCode }}"
                  equipment:
                    - affectedCustomers:
                        subscriberId: "{{ subscriberId }}"
                      olt: "{{ olt }}"
                      station: "{{ station }}"
              type: "Banda Larga"
            company: "CP_XPTO"
            emissionDate: "2022-06-28T12:16:00"
            eventType: "Lentiḍo"
            id: "210067783"
            identifiedFailure: "Rompimento de cabo"
            messageType: "Comunicado"
            promiseDate: "2022-06-30T16:20:40"
            startDate: "2022-06-29T12:05:00"
            status: "Abertura"
            ticketNumber: "43246567"
            type: "Incidente"
      - expect:
          statusCode: 204
      - expect:
          hasHeader: "Content-Type"
      - expect:
          headerEquals: ["Content-Type", "application/json"]
      - expect:
          hasProperty: "affectedService"
      - expect:
          hasProperty: "company"
      - expect:
          hasProperty: "emissionDate"
      - expect:
          hasProperty: "eventType"
      - expect:
          hasProperty: "id"
      - expect:
          hasProperty: "identifiedFailure"
      - expect:
          hasProperty: "messageType"
      - expect:
          hasProperty: "promiseDate"
      - expect:
          hasProperty: "startDate"
      - expect:
          hasProperty: "status"
      - expect:
          hasProperty: "ticketNumber"
      - expect:
          hasProperty: "type"
      - think: 2
      - log: "Completed POST request for incident opening notification"
