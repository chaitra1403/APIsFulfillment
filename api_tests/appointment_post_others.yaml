# ********RoostGPT********

# Test generated by RoostGPT for test Artillery using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /appointment_post for http method type POST in artillery framework
# RoostTestHash=c024d81ff3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "appointment_post_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Successful API Requests for Appointment"
    flow:
      - log: "Testing invalid payload with missing 'reason' field"
      - post:
          url: "/appointment"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            appointment:
              slot:
                id: "bff13821-73c3-42cc-9327-b05c7df5a67f"
      - expect:
          statusCode: 400
      - expect:
          hasProperty: "error"
      - expect:
          hasProperty: "message"
      - think: 2

      - log: "Testing invalid slot ID format"
      - post:
          url: "/appointment"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            appointment:
              reason: "Agendamento para Instalação de Fibra"
              slot:
                id: "invalid-slot-id"
      - expect:
          statusCode: 400
      - expect:
          hasProperty: "error"
      - expect:
          hasProperty: "message"
      - think: 2

      - log: "Testing unauthorized request with missing token"
      - post:
          url: "/appointment"
          headers:
            Content-Type: "application/json"
          json:
            appointment:
              reason: "Agendamento para Instalação de Fibra"
              slot:
                id: "bff13821-73c3-42cc-9327-b05c7df5a67f"
      - expect:
          statusCode: 401
      - expect:
          hasProperty: "error"
      - expect:
          hasProperty: "message"
      - think: 2

      - log: "Testing invalid URL"
      - post:
          url: "/invalid-appointment"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            appointment:
              reason: "Agendamento para Instalação de Fibra"
              slot:
                id: "bff13821-73c3-42cc-9327-b05c7df5a67f"
      - expect:
          statusCode: 404
      - expect:
          hasProperty: "error"
      - expect:
          hasProperty: "message"
      - think: 2
