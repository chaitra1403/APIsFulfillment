# ********RoostGPT********

# Test generated by RoostGPT for test new-test-321 using AI Type  and AI Model 
# 
# Test generated for /appointment_post for http method type POST in artillery framework
# RoostTestHash=f737d5caf2
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "appointment_post_others.csv"
    fields: ["reason", "slot_id"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    prod:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        pool: 10
scenarios:
  - name: "Test non-successful API responses"
    flow:
      - log: "Sending invalid POST request to /appointment"
      - post:
          url: "/appointment"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            appointment:
              reason: "{{ reason }}"
              slot:
                id: "{{ slot_id }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "message"
      - think: 1

      - log: "Sending POST request to /appointment with invalid token"
      - post:
          url: "/appointment"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer invalid_token"
          json:
            appointment:
              reason: "{{ reason }}"
              slot:
                id: "{{ slot_id }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "message"
      - think: 1

      - log: "Sending POST request to /appointment with missing slot id"
      - post:
          url: "/appointment"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            appointment:
              reason: "{{ reason }}"
              slot:
                id: ""
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "message"
      - think: 1

      - log: "Sending POST request to non-existing URL"
      - post:
          url: "/non_existing_url"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            appointment:
              reason: "{{ reason }}"
              slot:
                id: "{{ slot_id }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasProperty: "message"
      - think: 1
