# ********RoostGPT********

# Test generated by RoostGPT for test Artillery using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /appointmentConfirmation_post for http method type POST in artillery framework
# RoostTestHash=d3e6c8c6f7
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "appointmentConfirmation_post_success.csv"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Appointment Confirmation Scenario"
    flow:
      - log: "Starting Appointment Confirmation Test"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            appointment:
              confirmation: true
              id: "{{ appointmentId }}"
            customer:
              subscriberId: "{{ subscriberId }}"
      - expect:
          statusCode: 200
      - expect:
          contentType: "application/json"
      - expect:
          hasProperty: "control"
      - expect:
          hasProperty: "control.code"
      - expect:
          hasProperty: "control.message"
      - expect:
          hasProperty: "control.type"
      - expect:
          equals:
            "control.code": "200"
      - expect:
          equals:
            "control.message": "OK"
      - expect:
          equals:
            "control.type": "S"
      - think: 2
