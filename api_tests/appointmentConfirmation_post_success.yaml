# ********RoostGPT********

# Test generated by RoostGPT for test metering-1 using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /appointmentConfirmation_post for http method type POST in artillery framework
# RoostTestHash=d3e6c8c6f7
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.URL1_API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "appointmentConfirmation_post_success.csv"
    skipHeader: true
    delimiter: "^|^"
    fields:
      - confirmation
      - id
      - subscriberId
  http:
    headers:
      Authorization: "Bearer {{$processEnvironment.URL1_AUTH_TOKEN}}"

scenarios:
  - name: Validate successful responses of Appointment Confirmation API
    flow:
      - log: "Starting test scenario: Appointment Confirmation API"
      
      - log: "Executing POST request to confirm an appointment"
      
      - post:
          url: "/appointmentConfirmation"
          headers:
            Content-Type: "application/json"
          json:
            appointment:
              confirmation: "{{ confirmation }}"
              id: "{{ id }}"
            customer:
              subscriberId: "{{ subscriberId }}"
            
      - expect:
          - statusCode: 200
          - contentType: "application/json"
          - hasProperty: "control"
          - hasProperty: "control.code"
          - hasProperty: "control.message"
          - hasProperty: "control.type"
          - headerEquals:
              Content-Type: "application/json; charset=utf-8"
          - matchesRegexp:
              path: control.code
              pattern: "^[0-9]{3}$"
          - matchesRegexp:
              path: control.message
              pattern: "^[\\w\\W]{1,250}$"
          - jmespath:
              expression: control.type
              value: "S"
      
      - think: 2
