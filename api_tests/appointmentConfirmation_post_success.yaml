# ********RoostGPT********

# Test generated by RoostGPT for test new-test-321 using AI Type  and AI Model 
# 
# Test generated for /appointmentConfirmation_post for http method type POST in artillery framework
# RoostTestHash=1705b22338
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "appointmentConfirmation_post_success.csv"
    fields:
      - "confirmation"
      - "id"
      - "subscriberId"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Appointment Confirmation API test scenario"
    flow:
      - log: "Starting Appointment Confirmation API test scenario"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Content-Type: "application/json"
            authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            appointment:
              confirmation: "{{ confirmation }}"
              id: "{{ id }}"
            customer:
              subscriberId: "{{ subscriberId }}"
          expect:
            - statusCode: 200
            - hasProperty: "control"
            - hasProperty: "control.code"
            - equals: 
                - "control.code"
                - "200"
            - hasProperty: "control.message"
            - hasProperty: "control.type"
            - equals: 
                - "control.type"
                - "S"
      - think: 1
