# ********RoostGPT********

# Test generated by RoostGPT for test Artillery-test1-unit using AI Type  and AI Model 
# 
# Test generated for /products/{productId}_put for http method type PUT in artillery framework
# RoostTestHash=6762b8ac71
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.nurl1_API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_put_others.csv"
    skipHeader: true
  phases:
    - duration: 1
      arrivalRate: 1

scenarios:
  - name: Update Product Scenario
    flow:
      - log: "Starting Update Product request"
      - put:
          url: "/products/{{ productId }}"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.nurl1_AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
      - think: 2

      - log: "Starting Update Product request with invalid productId"
      - put:
          url: "/products/invalid"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.nurl1_AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 400
      - think: 2

      - log: "Starting Update Product request without authorization"
      - put:
          url: "/products/{{ productId }}"
          headers:
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 401
      - think: 2

      - log: "Starting Update Product request with invalid payload"
      - put:
          url: "/products/{{ productId }}"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.nurl1_AUTH_TOKEN }}"
          json:
            id: "invalid"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "invalid"
          expect:
            - statusCode: 400
      - think: 2
