# ********RoostGPT********

# Test generated by RoostGPT for test Artillery-test1-unit using AI Type  and AI Model 
# 
# Test generated for /products/{productId}_put for http method type PUT in artillery framework
# RoostTestHash=7b2a6b6959
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.url1_API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "products_productId_put_others.csv"
    fields:
      - "productId"
      - "name"
      - "description"
      - "price"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Update Product Scenario"
    flow:
      - log: "Starting Update Product test"
      - put:
          url: "/products/{{ productId }}"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.url1_AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
      - think: 2

      - log: "Testing with invalid product ID"
      - put:
          url: "/products/invalid_id"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.url1_AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Testing with missing required field (name)"
      - put:
          url: "/products/{{ productId }}"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.url1_AUTH_TOKEN }}"
          json:
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Testing with invalid price (negative value)"
      - put:
          url: "/products/{{ productId }}"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.url1_AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: -10
          expect:
            - statusCode: 400
      - think: 1

      - log: "Testing with invalid authorization"
      - put:
          url: "/products/{{ productId }}"
          headers:
            Content-Type: "application/json"
            Authorization: "InvalidToken"
          json:
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 401
      - think: 1
