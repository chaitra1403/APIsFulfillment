# ********RoostGPT********

# Test generated by RoostGPT for test Artillery-test2-unit-swagger using AI Type  and AI Model 
# 
# Test generated for /laureate/{laureateID}_get for http method type GET in artillery framework
# RoostTestHash=56bfcadf64
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "./laureate_laureateID_get_success.csv"
    fields: ["laureateID"]
    skipHeader: true
    delimiter: "^|^"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Successful API requests"
    flow:
      - log: "Starting GET request to /2.1/laureate/{laureateID}"
      - get:
          url: "/2.1/laureate/{{ laureateID }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "laureateID"
      - think: 1
