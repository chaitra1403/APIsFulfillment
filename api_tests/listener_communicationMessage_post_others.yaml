# ********RoostGPT********

# Test generated by RoostGPT for test new-postman_clone using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /listener/communicationMessage_post for http method type POST in artillery framework
# RoostTestHash=fdd5ae6c84
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "listener_communicationMessage_post_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Successful API Requests for Communication Message Listener"
    flow:
      - log: "Testing invalid payload structure"
      - post:
          url: "/listener/communicationMessage"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            invalidField: "Invalid data"
      - expect:
          - statusCode: 400
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2

      - log: "Testing missing required fields"
      - post:
          url: "/listener/communicationMessage"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            company: "CP_XPTO"
      - expect:
          - statusCode: 400
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2

      - log: "Testing invalid URL"
      - post:
          url: "/listener/invalidEndpoint"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            company: "CP_XPTO"
      - expect:
          - statusCode: 404
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2

      - log: "Testing unauthorized access"
      - post:
          url: "/listener/communicationMessage"
          headers:
            Content-Type: "application/json"
          json:
            company: "CP_XPTO"
      - expect:
          - statusCode: 401
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2

      - log: "Testing invalid data types"
      - post:
          url: "/listener/communicationMessage"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            company: 12345
            emissionDate: "invalid-date"
      - expect:
          - statusCode: 400
          - hasProperty: "error"
          - hasProperty: "message"
      - think: 2
