# ********RoostGPT********

# Test generated by RoostGPT for test Artillery_01 using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /listener/communicationMessage_post for http method type POST in artillery framework
# RoostTestHash=fdd5ae6c84
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "listener_communicationMessage_post_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Successful API Requests"
    flow:
      - log: "Testing invalid URL"
      - post:
          url: "/listener/invalidEndpoint"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            id: "210067783"
            company: "CP_XPTO"
            messageType: "Comunicado"
            status: "Abertura"
      - expect:
          statusCode: 404
      - expect:
          hasHeader: "Content-Type"
      - expect:
          contentType: "application/json"
      - think: 2

      - log: "Testing invalid payload structure"
      - post:
          url: "/listener/communicationMessage"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            invalidField: "invalidValue"
      - expect:
          statusCode: 400
      - expect:
          hasHeader: "Content-Type"
      - expect:
          contentType: "application/json"
      - think: 2

      - log: "Testing missing authorization"
      - post:
          url: "/listener/communicationMessage"
          headers:
            Content-Type: "application/json"
          payload:
            id: "210067783"
            company: "CP_XPTO"
            messageType: "Comunicado"
            status: "Abertura"
      - expect:
          statusCode: 401
      - expect:
          hasHeader: "Content-Type"
      - expect:
          contentType: "application/json"
      - think: 2

      - log: "Testing invalid data type for 'id'"
      - post:
          url: "/listener/communicationMessage"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            id: "123456789"
            company: "CP_XPTO"
            messageType: "Comunicado"
            status: "Abertura"
      - expect:
          statusCode: 400
      - expect:
          hasHeader: "Content-Type"
      - expect:
          contentType: "application/json"
      - think: 2

      - log: "Testing missing required field 'company'"
      - post:
          url: "/listener/communicationMessage"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            id: "210067783"
            messageType: "Comunicado"
            status: "Abertura"
      - expect:
          statusCode: 400
      - expect:
          hasHeader: "Content-Type"
      - expect:
          contentType: "application/json"
      - think: 2
