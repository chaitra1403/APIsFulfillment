# ********RoostGPT********

# Test generated by RoostGPT for test Artillery using AI Type Open AI and AI Model gpt-4o
# 
# Test generated for /appointmentConfirmation_post for http method type POST in artillery framework
# RoostTestHash=d3e6c8c6f7
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "appointmentConfirmation_post_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Successful API Response Tests for Appointment Confirmation"
    flow:
      - log: "Testing with invalid authentication token"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Authorization: "Bearer {{ $processEnvironment.INVALID_AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            appointment:
              confirmation: true
              id: "SA-562403"
            customer:
              subscriberId: "VTAL001"
      - expect:
          statusCode: 401
      - expect:
          hasHeader: "Content-Type"
      - expect:
          headerEquals:
            Content-Type: "application/json"
      - expect:
          hasProperty: "error"
      - expect:
          hasProperty: "message"
      - think: 2

      - log: "Testing with missing required fields in payload"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            appointment:
              confirmation: true
      - expect:
          statusCode: 400
      - expect:
          hasHeader: "Content-Type"
      - expect:
          headerEquals:
            Content-Type: "application/json"
      - expect:
          hasProperty: "error"
      - expect:
          hasProperty: "message"
      - think: 2

      - log: "Testing with invalid URL"
      - post:
          url: "/invalidEndpoint"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            appointment:
              confirmation: true
              id: "SA-562403"
            customer:
              subscriberId: "VTAL001"
      - expect:
          statusCode: 404
      - expect:
          hasHeader: "Content-Type"
      - expect:
          headerEquals:
            Content-Type: "application/json"
      - expect:
          hasProperty: "error"
      - expect:
          hasProperty: "message"
      - think: 2

      - log: "Testing with invalid data type for appointment ID"
      - post:
          url: "/appointmentConfirmation"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            appointment:
              confirmation: true
              id: 123456
            customer:
              subscriberId: "VTAL001"
      - expect:
          statusCode: 400
      - expect:
          hasHeader: "Content-Type"
      - expect:
          headerEquals:
            Content-Type: "application/json"
      - expect:
          hasProperty: "error"
      - expect:
          hasProperty: "message"
      - think: 2
